<UserControl x:Class="Presentation.WPFApp.Views.ProductListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Presentation.WPFApp.Views"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=viewmodels:ProductListViewModel}"
             xmlns:viewmodels="clr-namespace:Presentation.WPFApp.ViewModels"
             xmlns:models="clr-namespace:Infrastructure.Models;assembly=Infrastructure"
             Background="white"
             d:DesignHeight="800" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Text="Product List" Padding="5" FontSize="20" FontWeight="Bold" HorizontalAlignment="Left" Grid.Column="0" />
            <Button Content="New Product" Command="{Binding NavigateToAddProductCommand}" HorizontalAlignment="Right" Grid.Column="1" />
        </Grid>
        <ListView Grid.Row="1" ItemsSource="{Binding ProductList}" Margin="0 20 0 0">
            <ListView.ItemTemplate>
                <DataTemplate DataType="{x:Type models:Product}">
                    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Center">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"  />
                            <RowDefinition Height="auto" />
                            <RowDefinition Height="auto" />
                            <RowDefinition Height="auto" />
                            <RowDefinition Height="auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding Title}" />
                        <TextBlock Grid.Row="1" Grid.Column="0" Text="{Binding Price}" />
                        <TextBlock Grid.Row="2" Grid.Column="0" Text="{Binding Category.Name}" />
                        <TextBlock Grid.Row="3" Grid.Column="0" Text="{Binding Manufacturer.Name}" />
                        <Button Grid.Row="4" Content="Show" Grid.Column="1" CommandParameter="{Binding .}" Command="{Binding DataContext.NavigateToProductDetailsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" HorizontalAlignment="Right" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
